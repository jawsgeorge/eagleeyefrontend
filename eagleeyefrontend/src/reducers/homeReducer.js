export default function reducer(state={

},action){

 switch(action.type){
//    case "MeetingList" :{
//     return {...state, meetingList : action.meetingList,ClientFilterValue : localStorage.getItem("clinetFilter"),PeriodFilterValue : localStorage.getItem("periodFilter"),AppTimeZone : localStorage.getItem("timeZone")}
//    }
 }
return state;
}
